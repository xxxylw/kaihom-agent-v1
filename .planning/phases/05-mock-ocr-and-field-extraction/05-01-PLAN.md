---
phase: 05-mock-ocr-and-field-extraction
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/agent_task.py
  - app/schemas/order_draft.py
  - app/schemas/agent_tasks.py
  - app/services/mock_ocr.py
  - app/services/extraction.py
  - app/services/agent_tasks.py
  - app/api/agent_tasks.py
  - tests/test_extraction.py
  - tests/test_agent_tasks.py
autonomous: true
requirements:
  - EXTR-01
  - EXTR-02
  - EXTR-03
  - EXTR-04
  - EXTR-05
must_haves:
  truths:
    - Phase 5 defines a typed logistics order draft schema covering all agreed core, optional, and international reserved fields.
    - Required-field detection only treats the first-layer core fields as required in Phase 5.
    - Second-layer and third-layer fields exist in the schema and preview, but may be null/missing without blocking extraction.
    - Draft preview, missing fields, and extraction metadata are persisted as JSON on/near the Agent task.
    - The implementation preserves stable canonical field names and evidence metadata so future query/reporting projections can be built without redoing extraction.
    - `POST /agent/tasks/{task_id}/extract` triggers deterministic mock OCR and extraction for authenticated task owners.
    - Extraction transitions task status from `created` to `extracting`, then to `need_more_info` or `ready_for_review`.
    - Phase 5 records business-level extraction events.
    - Phase 5 does not generate clarification questions, finalize drafts, call real OCR/LLM services, or introduce LangChain/LangGraph runtime dependencies.
  artifacts:
    - `app/schemas/order_draft.py` defines typed draft fields, evidence, missing fields, conflicts, and preview schemas.
    - `app/services/mock_ocr.py` provides deterministic OCR fixture text for uploaded files.
    - `app/services/extraction.py` extracts typed draft fields from mock OCR text.
    - `app/models/agent_task.py` can persist extraction preview JSON.
    - `app/services/agent_tasks.py` orchestrates extraction and task status/events.
    - `app/api/agent_tasks.py` exposes the protected extraction action endpoint.
    - `tests/test_extraction.py` and `tests/test_agent_tasks.py` verify extraction behavior and API integration.
  key_links:
    - Extraction consumes `AgentTaskRecord.file_ids_json` and linked `UploadedFileRecord` rows from Phase 4.
    - Task detail responses reuse `AgentTaskResponse.draft_preview`.
    - Phase 6 will consume `missing_fields` to generate clarification questions.
    - Future reporting/query phases can promote canonical JSON fields into normalized projection tables or views.
---

<objective>
Implement Phase 5 mock OCR and field extraction for Kaihom Agent v1.

Purpose: Convert deterministic mock OCR text from uploaded logistics documents into a structured draft preview, identify missing required fields, persist extraction results on the Agent task, and make task detail show the extracted preview.
Output: Typed Pydantic draft schema, deterministic mock OCR fixtures, extraction service, protected extraction endpoint, task JSON persistence, business events, and tests.
</objective>

<execution_context>
@C:/Users/qirui.huang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/qirui.huang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/05-mock-ocr-and-field-extraction/05-CONTEXT.md
@.planning/phases/05-mock-ocr-and-field-extraction/05-RESEARCH.md
@.planning/phases/04-agent-task-state-machine/04-CONTEXT.md
@.planning/phases/04-agent-task-state-machine/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add typed order draft extraction schemas</name>
  <files>app/schemas/order_draft.py, app/schemas/agent_tasks.py</files>
  <read_first>app/schemas/agent_tasks.py, app/schemas/mock_kaihong.py, .planning/phases/05-mock-ocr-and-field-extraction/05-CONTEXT.md</read_first>
  <action>
    Create `app/schemas/order_draft.py`.
    Define canonical field constants for all agreed fields:
    - Required in Phase 5: `customer_name`, `shipper_name`, `shipper_phone`, `shipper_address`, `consignee_name`, `consignee_phone`, `consignee_address`, `cargo_name`, `package_count`, `gross_weight_kg`.
    - Optional v1: `origin`, `destination`, `cargo_volume_cbm`, `package_type`, `transport_mode`, `service_type`, `pickup_date`, `remarks`.
    - Optional international/reserved: `hs_code`, `declared_value`, `currency`, `incoterm`, `invoice_no`, `container_type`, `container_count`, `booking_no`, `bill_of_lading_no`.
    Add a typed `OrderDraftFields` Pydantic model with these fields. Use strings for text fields, numeric types for counts/weights/volume/value, and nullable defaults.
    Add `FieldEvidence` with `source_file_id`, `source_filename`, `source_text`, and `confidence`.
    Add `FieldConflict` with `field_name`, `kept_value`, `conflicting_value`, and source metadata.
    Add `OrderDraftPreview` with `fields`, `missing_fields`, `field_evidence`, and `conflicts`.
    Keep schema typed; avoid arbitrary dict-only field storage for business fields.
    Update `app/schemas/agent_tasks.py` so `draft_preview` can use `OrderDraftPreview | None` instead of a loose dict if this does not create circular imports.
  </action>
  <verify>python -c "from app.schemas.order_draft import OrderDraftFields, OrderDraftPreview, REQUIRED_DRAFT_FIELDS; print(len(REQUIRED_DRAFT_FIELDS), OrderDraftFields().__class__.__name__, OrderDraftPreview.__name__)"</verify>
  <acceptance_criteria>
    - All 27 agreed business fields exist in `OrderDraftFields`.
    - Required-field list contains exactly the 10 Phase 5 required fields.
    - Evidence and conflict schemas exist.
    - Task response can expose a typed draft preview.
    - Canonical field names are centralized for future query/reporting projections.
  </acceptance_criteria>
  <done>Draft extraction has a stable typed schema that covers current and future logistics fields.</done>
</task>

<task type="auto">
  <name>Task 2: Add Agent task extraction persistence</name>
  <files>app/models/agent_task.py, app/services/agent_tasks.py</files>
  <read_first>app/models/agent_task.py, app/services/agent_tasks.py, app/db/session.py</read_first>
  <action>
    Extend `AgentTaskRecord` with nullable JSON text columns:
    - `draft_preview_json`
    - `missing_fields_json`
    - `extraction_result_json`
    Keep this as JSON persistence for Phase 5 preview data, not one database column per logistics field.
    In `app/services/agent_tasks.py`, update `_task_to_response` so it returns a populated `draft_preview` when `draft_preview_json` exists.
    Ensure existing tasks without extraction data still return `draft_preview = None` and `questions = []`.
    Preserve current task creation and event behavior.
  </action>
  <verify>python -c "from app.models.agent_task import AgentTaskRecord; print(hasattr(AgentTaskRecord, 'draft_preview_json'), hasattr(AgentTaskRecord, 'missing_fields_json'), hasattr(AgentTaskRecord, 'extraction_result_json'))"</verify>
  <acceptance_criteria>
    - Agent tasks can persist preview, missing fields, and extraction metadata.
    - Existing Phase 4 behavior remains backward compatible for unextracted tasks.
    - Task detail returns the preview after extraction and remains null before extraction.
    - Storage choice is JSON but schema names stay stable for future query projection.
  </acceptance_criteria>
  <done>Agent task records can store and return extraction preview JSON.</done>
</task>

<task type="auto">
  <name>Task 3: Implement deterministic mock OCR fixtures</name>
  <files>app/services/mock_ocr.py, tests/test_extraction.py</files>
  <read_first>app/models/uploaded_file.py, app/services/uploads.py, .planning/phases/05-mock-ocr-and-field-extraction/05-RESEARCH.md</read_first>
  <action>
    Create `app/services/mock_ocr.py`.
    Add a small deterministic function such as `get_mock_ocr_text(upload: UploadedFileRecord) -> str`.
    Use simple fixture selection based on `document_type`, `original_filename`, or file kind.
    Include at least:
    - One complete logistics fixture that contains all Phase 5 required fields.
    - One incomplete fixture missing several required fields.
    - One international/ocean-style fixture containing optional reserved fields such as `hs_code`, `invoice_no`, `container_type`, or `bill_of_lading_no`.
    Do not read real file bytes for OCR in Phase 5.
    Add tests proving fixture selection is deterministic and does not call external services.
  </action>
  <verify>python -m pytest tests/test_extraction.py::test_mock_ocr_fixture_is_deterministic -q</verify>
  <acceptance_criteria>
    - Mock OCR returns deterministic text from upload metadata.
    - Complete, incomplete, and international fixture cases exist.
    - No real OCR, network call, LLM call, LangChain, or LangGraph dependency is used.
  </acceptance_criteria>
  <done>Phase 5 has stable mock OCR text inputs for repeatable extraction tests.</done>
</task>

<task type="auto">
  <name>Task 4: Implement deterministic extraction service</name>
  <files>app/services/extraction.py, tests/test_extraction.py</files>
  <read_first>app/schemas/order_draft.py, app/services/mock_ocr.py, tests/test_mock_kaihong.py</read_first>
  <action>
    Create `app/services/extraction.py`.
    Implement a deterministic parser that accepts one or more OCR text inputs with source file metadata and returns `OrderDraftPreview`.
    Use simple key-value rules that match fixture labels such as `Customer:`, `Shipper Name:`, `Cargo:`, `Packages:`, `Gross Weight KG:`, `Origin:`, `Destination:`, `HS Code:`, and similar Chinese labels where useful.
    Convert numeric fields to numbers:
    - `package_count` to integer.
    - `gross_weight_kg`, `cargo_volume_cbm`, and `declared_value` to floats.
    Populate field evidence for extracted fields.
    Compute `missing_fields` from `REQUIRED_DRAFT_FIELDS`.
    For duplicate conflicting values across files, keep the first value and record a `FieldConflict`.
    Add focused tests for:
    - Complete fixture produces `ready`-quality preview with no required missing fields.
    - Incomplete fixture produces expected `missing_fields`.
    - Optional international fields are extracted when present.
    - Conflict metadata is recorded while first value is retained.
  </action>
  <verify>python -m pytest tests/test_extraction.py -q</verify>
  <acceptance_criteria>
    - EXTR-01 is covered by typed schema use.
    - EXTR-02 is covered by mock OCR fixture inputs.
    - EXTR-03 is covered by deterministic field extraction.
    - EXTR-04 is covered by missing required field detection.
    - EXTR-05 is covered by evidence and conflict metadata.
  </acceptance_criteria>
  <done>Extraction service converts mock OCR text into a typed preview with missing-field and evidence metadata.</done>
</task>

<task type="auto">
  <name>Task 5: Add protected task extraction workflow endpoint</name>
  <files>app/services/agent_tasks.py, app/api/agent_tasks.py, tests/test_agent_tasks.py</files>
  <read_first>app/services/agent_tasks.py, app/api/agent_tasks.py, app/services/extraction.py, app/services/mock_ocr.py, tests/test_agent_tasks.py</read_first>
  <action>
    In `app/services/agent_tasks.py`, add extraction event constants:
    - `extraction_started`
    - `mock_ocr_completed`
    - `fields_extracted`
    - `missing_fields_detected`
    Implement a service function such as `extract_agent_task(session, task_id, current_username)`.
    Behavior:
    - Return not found/inaccessible when task is missing or owned by another user.
    - Allow extraction from `created`; reject finalized tasks and invalid states with a clear service error.
    - Transition `created -> extracting`.
    - Load linked `UploadedFileRecord` rows.
    - Generate mock OCR text for each file.
    - Run extraction service.
    - Persist `draft_preview_json`, `missing_fields_json`, and `extraction_result_json`.
    - Transition to `need_more_info` when required fields are missing; otherwise `ready_for_review`.
    - Record business-level events.
    In `app/api/agent_tasks.py`, add `POST /agent/tasks/{task_id}/extract` protected by existing bearer auth.
    Return the updated `AgentTaskResponse`.
    Do not add natural-language clarification questions in Phase 5; keep `questions = []`.
    Add integration tests:
    - Unauthenticated extract returns 401.
    - Extracting a complete fixture returns `ready_for_review` and a populated preview.
    - Extracting an incomplete fixture returns `need_more_info` and expected missing fields.
    - Task detail after extraction returns the saved preview.
    - Events include extraction business events.
    - Finalized or invalid-state tasks cannot be extracted again accidentally.
  </action>
  <verify>python -m pytest tests/test_agent_tasks.py -q</verify>
  <acceptance_criteria>
    - `POST /agent/tasks/{task_id}/extract` exists in OpenAPI.
    - Endpoint requires authentication.
    - Extraction updates task status deterministically.
    - Task detail returns populated `draft_preview`.
    - `questions` remains empty in Phase 5.
    - Business-level extraction events are queryable.
    - LangChain/LangGraph are not runtime dependencies.
  </acceptance_criteria>
  <done>Agent tasks can be extracted through a protected workflow action endpoint.</done>
</task>

<task type="auto">
  <name>Task 6: Verify query-readiness and regression safety</name>
  <files>tests/test_extraction.py, tests/test_agent_tasks.py</files>
  <read_first>app/schemas/order_draft.py, app/services/extraction.py, app/services/agent_tasks.py, .planning/phases/05-mock-ocr-and-field-extraction/05-CONTEXT.md</read_first>
  <action>
    Add or refine tests that protect future query/reporting readiness:
    - Assert every canonical field appears on `OrderDraftFields`.
    - Assert extracted preview JSON uses canonical field names.
    - Assert evidence metadata includes source file identifiers.
    - Assert optional fields can be null without appearing in `missing_fields`.
    - Assert advanced fields such as `hs_code`, `container_type`, and `bill_of_lading_no` can be represented when present.
    Add OpenAPI assertions for the extraction endpoint and typed preview response where practical.
    Run focused and full test suites.
  </action>
  <verify>python -m pytest tests/test_extraction.py tests/test_agent_tasks.py -q && python -m pytest -q</verify>
  <acceptance_criteria>
    - Canonical field coverage is test-protected.
    - Future reporting/query projection can rely on stable field names and evidence metadata.
    - Existing Phase 1-4 tests continue passing.
  </acceptance_criteria>
  <done>Phase 5 implementation is regression-tested and preserves future analytics/query evolution paths.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `python -m pytest tests/test_extraction.py -q`.
- [ ] Run `python -m pytest tests/test_agent_tasks.py -q`.
- [ ] Run `python -m pytest -q`.
- [ ] Run `python -c "from app.main import app; print('/agent/tasks/{task_id}/extract' in app.openapi()['paths'])"`.
- [ ] Inspect the generated `draft_preview` from a manual UAT task and confirm it contains `fields`, `missing_fields`, `field_evidence`, and `conflicts`.
- [ ] Confirm no LangChain/LangGraph packages were added to `pyproject.toml`.
</verification>

<success_criteria>

- EXTR-01 through EXTR-05 are addressed.
- Core draft schema exists and includes required, optional, and reserved international fields.
- Mock OCR fixtures drive extraction deterministically.
- Missing required fields are detected.
- Draft preview includes extracted field values and evidence metadata.
- Agent task status and events reflect extraction progress.
- Storage remains JSON for Phase 5 preview data while preserving canonical fields for future query/reporting projections.

</success_criteria>

<output>
After completion, create `.planning/phases/05-mock-ocr-and-field-extraction/05-01-SUMMARY.md`.
</output>

## PLANNING COMPLETE
