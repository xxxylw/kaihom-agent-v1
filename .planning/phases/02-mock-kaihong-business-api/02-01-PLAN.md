---
phase: 02-mock-kaihong-business-api
plan: 01
type: execute
wave: 1
depends_on:
  - 01-fastapi-foundation
files_modified:
  - .gitignore
  - app/main.py
  - app/core/config.py
  - app/db/__init__.py
  - app/db/session.py
  - app/models/__init__.py
  - app/models/draft.py
  - app/schemas/__init__.py
  - app/schemas/mock_kaihong.py
  - app/services/__init__.py
  - app/services/mock_kaihong.py
  - app/api/mock_kaihong.py
  - tests/test_mock_kaihong.py
autonomous: true
requirements:
  - MOCK-01
  - MOCK-02
  - MOCK-03
  - MOCK-04
  - MOCK-05
user_setup:
  - None. The phase uses local SQLite and deterministic mock data only.
must_haves:
  truths:
    - Mock login returns a bearer token for a seeded staff user.
    - Current-user endpoint returns the seeded staff profile when called with the mock bearer token.
    - Customer list/detail, dictionaries, and recent-order endpoints are documented and test-covered.
    - Drafts can be saved and retrieved locally through Mock Kaihong endpoints.
    - Existing `/health` and OpenAPI tests still pass.
  artifacts:
    - `app/api/mock_kaihong.py` defines the Mock Kaihong router.
    - `app/schemas/mock_kaihong.py` defines request/response contracts.
    - `app/services/mock_kaihong.py` defines deterministic mock business data and service helpers.
    - `app/db/session.py` defines local SQLite engine/session dependency.
    - `app/models/draft.py` defines the SQLModel draft table.
    - `tests/test_mock_kaihong.py` verifies all MOCK-01 through MOCK-05 behavior.
  key_links:
    - `app/main.py` includes the Mock Kaihong router.
    - Draft persistence uses the same session dependency that later phases can reuse.
---

<objective>
Create the Mock Kaihong business API boundary for Kaihom Agent v1.

Purpose: Let later upload, Agent task, extraction, and draft-finalization phases work against realistic business endpoints before real Kaihong Wing access exists.
Output: Mock auth/current-user, customers, dictionaries, recent orders, draft persistence, OpenAPI contracts, and tests.
</objective>

<execution_context>
@C:/Users/qirui.huang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/qirui.huang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/01-fastapi-foundation/01-01-SUMMARY.md
@.planning/phases/01-fastapi-foundation/01-UAT.md
@.planning/phases/02-mock-kaihong-business-api/02-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add local database foundation for mock drafts</name>
  <files>.gitignore, app/core/config.py, app/db/__init__.py, app/db/session.py, app/models/__init__.py, app/models/draft.py</files>
  <read_first>app/core/config.py, app/main.py, .planning/phases/02-mock-kaihong-business-api/02-RESEARCH.md</read_first>
  <action>
    Use TDD even though the phase config is not globally TDD-enforced.

    First create `tests/test_mock_kaihong.py` with a failing draft-persistence-focused test scaffold that imports the future API app and will later verify saving and retrieving a draft.

    Add `app/core/config.py` setting:
    - `database_url: str = "sqlite:///./kaihom_agent_local.db"`

    Create `app/db/session.py`:
    - Import `Session`, `SQLModel`, and `create_engine` from `sqlmodel`.
    - Import `get_settings`.
    - Create `engine = create_engine(get_settings().database_url, connect_args={"check_same_thread": False})`.
    - Define `create_db_and_tables() -> None` calling `SQLModel.metadata.create_all(engine)`.
    - Define `get_session()` yielding a `Session(engine)`.

    Create `app/models/draft.py`:
    - SQLModel table class `OrderDraftRecord`.
    - Fields: `id: str` primary key, `customer_id: str`, `created_by: str`, `status: str = "draft"`, `payload_json: str`, `source: str = "mock-kaihong"`, `created_at: datetime`, `updated_at: datetime`.
    - Use `default_factory=datetime.utcnow` for timestamps.

    Create package markers for `app/db/` and `app/models/`.

    Update `.gitignore` to exclude `*.db`, `*.sqlite`, and `*.sqlite3`.
  </action>
  <verify>Test-Path app/db/session.py; Test-Path app/models/draft.py; Select-String -Path app/core/config.py -Pattern "database_url"; Select-String -Path .gitignore -Pattern "*.db"</verify>
  <acceptance_criteria>
    - `app/db/session.py` contains `create_db_and_tables`.
    - `app/db/session.py` contains `get_session`.
    - `app/models/draft.py` contains `class OrderDraftRecord`.
    - `app/core/config.py` contains `database_url`.
    - `.gitignore` excludes local SQLite database files.
  </acceptance_criteria>
  <done>Local draft storage foundation exists and is isolated to the mock/local data boundary.</done>
</task>

<task type="auto">
  <name>Task 2: Define Mock Kaihong schemas and deterministic service data</name>
  <files>app/schemas/__init__.py, app/schemas/mock_kaihong.py, app/services/__init__.py, app/services/mock_kaihong.py, tests/test_mock_kaihong.py</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/phases/02-mock-kaihong-business-api/02-RESEARCH.md</read_first>
  <action>
    Extend `tests/test_mock_kaihong.py` with failing tests for:
    - `POST /mock/kaihong/auth/login` returns a bearer token.
    - `GET /mock/kaihong/users/me` returns the seeded current user when the bearer token is provided.
    - `GET /mock/kaihong/customers` returns seeded customers and supports a `q` filter.
    - `GET /mock/kaihong/customers/{customer_id}` returns one seeded customer.
    - `GET /mock/kaihong/dictionaries` returns `ports`, `warehouses`, `yards`, and `fee_types`.
    - `GET /mock/kaihong/customers/{customer_id}/recent-orders` returns customer-specific recent orders.

    Create `app/schemas/mock_kaihong.py` with Pydantic models:
    - `MockLoginRequest(username: str, password: str)`
    - `MockTokenResponse(access_token: str, token_type: str = "bearer")`
    - `MockUserResponse(id: str, username: str, display_name: str, department: str)`
    - `MockCustomerResponse(id: str, name: str, short_name: str | None, contact_name: str | None, contact_phone: str | None)`
    - `MockDictionaryItem(code: str, name: str)`
    - `MockDictionariesResponse(ports: list[MockDictionaryItem], warehouses: list[MockDictionaryItem], yards: list[MockDictionaryItem], fee_types: list[MockDictionaryItem])`
    - `MockRecentOrderResponse(id: str, customer_id: str, route: str, cargo_name: str, container_type: str, created_at: datetime)`
    - `MockDraftCreateRequest(customer_id: str, created_by: str, payload: dict[str, Any], source: str = "mock-kaihong")`
    - `MockDraftResponse(draft_id: str, customer_id: str, created_by: str, status: str, payload: dict[str, Any], source: str, created_at: datetime, updated_at: datetime)`

    Create `app/services/mock_kaihong.py` with deterministic seed data:
    - One user: `yw001`, password `mock123456`, display name `业务员一号`.
    - Two or more customers relevant to logistics.
    - At least two entries for each dictionary list.
    - At least one recent order per seeded customer.

    Service helper functions:
    - `authenticate_user(username: str, password: str) -> MockUserResponse | None`
    - `token_for_user(user: MockUserResponse) -> str`
    - `get_user_by_token(token: str) -> MockUserResponse | None`
    - `list_customers(q: str | None = None) -> list[MockCustomerResponse]`
    - `get_customer(customer_id: str) -> MockCustomerResponse | None`
    - `get_dictionaries() -> MockDictionariesResponse`
    - `list_recent_orders(customer_id: str) -> list[MockRecentOrderResponse]`
  </action>
  <verify>Select-String -Path app/schemas/mock_kaihong.py -Pattern "MockLoginRequest","MockDraftCreateRequest"; Select-String -Path app/services/mock_kaihong.py -Pattern "yw001","list_customers","get_dictionaries"</verify>
  <acceptance_criteria>
    - `app/schemas/mock_kaihong.py` defines all request/response models listed above.
    - `app/services/mock_kaihong.py` contains seeded user `yw001`.
    - Customer search is deterministic and case-insensitive for `q`.
    - Dictionary response includes ports, warehouses, yards, and fee types.
    - Missing customer lookup can be represented as `None` for router-level 404 handling.
  </acceptance_criteria>
  <done>Mock Kaihong API contracts and deterministic business data are ready for router implementation.</done>
</task>

<task type="auto">
  <name>Task 3: Implement Mock Kaihong router and draft persistence</name>
  <files>app/api/mock_kaihong.py, app/main.py, app/services/mock_kaihong.py, tests/test_mock_kaihong.py</files>
  <read_first>app/main.py, app/api/health.py, app/db/session.py, app/models/draft.py, app/schemas/mock_kaihong.py, app/services/mock_kaihong.py</read_first>
  <action>
    Implement `app/api/mock_kaihong.py`:
    - `router = APIRouter(prefix="/mock/kaihong", tags=["Mock Kaihong"])`.
    - Add a bearer-token helper using FastAPI `Header` to read `Authorization`.
    - Return `401` when token is missing or invalid.
    - Return `404` for missing customer or missing draft.

    Routes:
    - `POST /auth/login`, response model `MockTokenResponse`.
    - `GET /users/me`, response model `MockUserResponse`.
    - `GET /customers`, response model `list[MockCustomerResponse]`, query param `q: str | None = None`.
    - `GET /customers/{customer_id}`, response model `MockCustomerResponse`.
    - `GET /dictionaries`, response model `MockDictionariesResponse`.
    - `GET /customers/{customer_id}/recent-orders`, response model `list[MockRecentOrderResponse]`.
    - `POST /drafts`, response model `MockDraftResponse`, status `201`.
    - `GET /drafts/{draft_id}`, response model `MockDraftResponse`.

    Add draft helper functions to `app/services/mock_kaihong.py`:
    - `create_draft(session: Session, request: MockDraftCreateRequest) -> MockDraftResponse`
    - `get_draft(session: Session, draft_id: str) -> MockDraftResponse | None`
    - Store payload as JSON in `OrderDraftRecord.payload_json`.
    - Generate draft IDs as `draft_` plus a UUID hex prefix.

    Update `app/main.py`:
    - Import and call `create_db_and_tables()` during app creation.
    - Include the Mock Kaihong router.

    Finish `tests/test_mock_kaihong.py` so all Phase 2 behavior is tested through `TestClient(app)`.
    Tests must cover:
    - Login success and failure.
    - Current user success with token and unauthorized without token.
    - Customers list/filter/detail.
    - Dictionaries contain the four groups.
    - Recent orders for a seeded customer.
    - Draft create then retrieve returns the same payload.
    - `/openapi.json` includes `/mock/kaihong/auth/login` and `/mock/kaihong/drafts`.
  </action>
  <verify>python -m pytest</verify>
  <acceptance_criteria>
    - `app/main.py` includes the Mock Kaihong router.
    - `POST /mock/kaihong/auth/login` returns HTTP 200 with a bearer token for valid seeded credentials.
    - `GET /mock/kaihong/users/me` returns HTTP 401 without a valid bearer token.
    - `GET /mock/kaihong/customers` and customer detail work.
    - `GET /mock/kaihong/dictionaries` returns ports, warehouses, yards, and fee types.
    - `GET /mock/kaihong/customers/{customer_id}/recent-orders` works for seeded customers.
    - `POST /mock/kaihong/drafts` followed by `GET /mock/kaihong/drafts/{draft_id}` preserves the payload.
    - `python -m pytest` passes all tests.
  </acceptance_criteria>
  <done>Mock Kaihong API is routable, documented by OpenAPI, and test-covered end to end.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `python -m pytest`.
- [ ] Run `python -c "from app.main import app; print(sorted(app.openapi()['paths'].keys()))"` and confirm Mock Kaihong paths are present.
- [ ] Start `python -m uvicorn app.main:app --reload --host 0.0.0.0 --port 8000` and open `/docs` if doing manual UAT.
</verification>

<success_criteria>

- All MOCK-01 through MOCK-05 requirements are addressed by concrete routes, schemas, services, and tests.
- Mock business data is deterministic and usable by later Agent workflow phases.
- Draft persistence is local and isolated from any real Kaihong Wing system.
- OpenAPI documents the Mock Kaihong contracts.
- No file upload, Agent state machine, OCR extraction, clarification loop, finalization workflow, real Java adapter, or real company database integration is implemented ahead of its phase.

</success_criteria>

<output>
After completion, create `.planning/phases/02-mock-kaihong-business-api/02-01-SUMMARY.md`.
</output>

## PLANNING COMPLETE
