---
phase: 01-fastapi-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - pyproject.toml
  - README.md
  - app/__init__.py
  - app/main.py
  - app/api/__init__.py
  - app/api/health.py
  - app/core/__init__.py
  - app/core/config.py
  - tests/test_health.py
autonomous: true
requirements:
  - FOUND-01
  - FOUND-02
  - FOUND-03
  - FOUND-04
user_setup:
  - Create and activate a Python 3.11+ virtual environment before installing dependencies if one is not already active.
must_haves:
  truths:
    - `uvicorn app.main:app --reload` can import the app without errors.
    - `GET /health` returns HTTP 200 with a healthy status payload.
    - FastAPI OpenAPI schema includes the health route.
    - `pytest` runs the foundation tests successfully.
  artifacts:
    - `pyproject.toml` exists with FastAPI, Uvicorn, Pydantic settings, pytest, and httpx/TestClient-compatible dependencies.
    - `app/main.py` exposes module-level `app`.
    - `app/api/health.py` defines the health endpoint.
    - `tests/test_health.py` verifies health and OpenAPI.
    - `README.md` documents setup, test, and run commands.
  key_links:
    - `app/main.py` includes the health router from `app/api/health.py`.
    - Tests import the same app object used by Uvicorn.
---

<objective>
Create the runnable FastAPI foundation for Kaihom Agent v1.

Purpose: Establish the backend skeleton that every later Mock Kaihong, upload, Agent task, extraction, and mobile demo phase will extend.
Output: Python project metadata, app package structure, health route, tests, and development commands.
</objective>

<execution_context>
@C:/Users/qirui.huang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/qirui.huang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/research/STACK.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-fastapi-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create Python project metadata and package skeleton</name>
  <files>pyproject.toml, app/__init__.py, app/api/__init__.py, app/core/__init__.py</files>
  <read_first>.planning/research/STACK.md, .planning/research/ARCHITECTURE.md</read_first>
  <action>
    Create `pyproject.toml` for a Python 3.11+ FastAPI application named `kaihom-agent-v1`.
    Include runtime dependencies: `fastapi`, `uvicorn[standard]`, `pydantic-settings`, `sqlmodel`, and `python-multipart`.
    Include dev/test dependencies in an optional dependency group named `dev`: `pytest` and `httpx`.
    Add pytest configuration so tests are discovered under `tests/`.
    Create package marker files for `app/`, `app/api/`, and `app/core/`.
    Do not add database models or upload logic in this phase; those belong to later phases.
  </action>
  <verify>Test-Path pyproject.toml; Test-Path app/__init__.py; Test-Path app/api/__init__.py; Test-Path app/core/__init__.py</verify>
  <acceptance_criteria>
    - `pyproject.toml` contains `fastapi`.
    - `pyproject.toml` contains `uvicorn[standard]`.
    - `pyproject.toml` contains `python-multipart`.
    - Package marker files exist under `app/`, `app/api/`, and `app/core/`.
  </acceptance_criteria>
  <done>Project metadata and Python package skeleton are present and scoped to foundation work.</done>
</task>

<task type="auto">
  <name>Task 2: Implement app settings, app factory, and health route</name>
  <files>app/core/config.py, app/api/health.py, app/main.py</files>
  <read_first>.planning/PROJECT.md, .planning/phases/01-fastapi-foundation/01-RESEARCH.md</read_first>
  <action>
    Create `app/core/config.py` with a small `Settings` class using `pydantic_settings.BaseSettings`.
    Include `app_name` defaulting to `Kaihom Agent API`, `app_version` defaulting to `0.1.0`, and `environment` defaulting to `local`.
    Expose `get_settings()` using `functools.lru_cache`.
    Create `app/api/health.py` with an `APIRouter` and `GET /health` endpoint that returns JSON containing `status: "healthy"`, `service`, `version`, and `environment`.
    Create `app/main.py` with `create_app()` and a module-level `app = create_app()`.
    Configure FastAPI title as `Kaihom Agent API`, description as `Mock-first logistics order entry Agent backend`, and version from settings.
    Include the health router.
  </action>
  <verify>python -c "from app.main import app; print(app.title)"</verify>
  <acceptance_criteria>
    - `app/main.py` contains `def create_app`.
    - `app/main.py` contains `app = create_app()`.
    - `app/api/health.py` contains `@router.get("/health"`.
    - `/health` payload includes `status`, `service`, `version`, and `environment`.
  </acceptance_criteria>
  <done>FastAPI app can be imported and exposes health routing through the same `app` object Uvicorn uses.</done>
</task>

<task type="auto">
  <name>Task 3: Add foundation tests and developer README commands</name>
  <files>tests/test_health.py, README.md</files>
  <read_first>.planning/REQUIREMENTS.md, .planning/ROADMAP.md</read_first>
  <action>
    Create `tests/test_health.py` using `fastapi.testclient.TestClient` against `app.main.app`.
    Add tests for:
    1. `GET /health` returns HTTP 200 and `status == "healthy"`.
    2. `GET /openapi.json` returns HTTP 200 and includes `/health` in `paths`.
    Update `README.md` with:
    - Project purpose in one paragraph.
    - Local setup commands for Windows PowerShell using `.venv`.
    - Dependency install command using `pip install -e ".[dev]"`.
    - Test command `pytest`.
    - Run command `uvicorn app.main:app --reload --host 0.0.0.0 --port 8000`.
    - Note that current scope is Mock-only and does not connect to Kaihong Wing.
  </action>
  <verify>pytest</verify>
  <acceptance_criteria>
    - `tests/test_health.py` imports `TestClient`.
    - Tests verify both `/health` and `/openapi.json`.
    - `README.md` contains `uvicorn app.main:app`.
    - `README.md` contains `pip install -e ".[dev]"`.
    - `README.md` says the project is Mock-only for now.
  </acceptance_criteria>
  <done>Foundation behavior is covered by tests and local developer commands are documented.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `python -c "from app.main import app; print(app.title)"`.
- [ ] Run `pytest`.
- [ ] Optionally run `uvicorn app.main:app --reload --host 0.0.0.0 --port 8000` and open `/docs`.
</verification>

<success_criteria>

- All FOUND-01 through FOUND-04 requirements are addressed by concrete files and checks.
- FastAPI application imports successfully.
- `/health` and OpenAPI are test-covered.
- README gives a new developer enough instructions to run the app locally.
- No Mock Kaihong, upload, database, Agent workflow, OCR, or mobile UI logic is implemented ahead of its phase.

</success_criteria>

<output>
After completion, create `.planning/phases/01-fastapi-foundation/01-01-SUMMARY.md`.
</output>

## PLANNING COMPLETE
