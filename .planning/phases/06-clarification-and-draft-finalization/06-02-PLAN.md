---
phase: 06-clarification-and-draft-finalization
plan: 02
type: execute
wave: 2
depends_on:
  - 06-01
files_modified:
  - app/models/agent_task.py
  - app/db/session.py
  - app/schemas/agent_tasks.py
  - app/schemas/clarification.py
  - app/services/clarification.py
  - app/services/agent_tasks.py
  - app/api/agent_tasks.py
  - tests/test_clarification_api.py
  - tests/test_agent_tasks.py
autonomous: true
requirements:
  - CLAR-01
  - CLAR-02
  - CLAR-03
  - CLAR-04
  - DRAFT-01
  - DRAFT-02
  - DRAFT-03
  - DRAFT-04
must_haves:
  truths:
    - D-01 The public upload/task/extract flow remains stable; Phase 6 starts from `draft_preview`.
    - D-06 The protected API shape is `GET /agent/tasks/{task_id}/clarification`, `POST /agent/tasks/{task_id}/clarification/answers`, and `POST /agent/tasks/{task_id}/finalize`.
    - D-07 Questions can group related fields, but requested fields/conflicts remain structured.
    - D-09 Backend code validates and merges model output before mutating draft state.
    - `ready_for_review` is reached only after required missing fields are complete and blocking conflicts are resolved.
    - `finalized` saves the complete draft through the existing Mock Kaihong boundary and returns a `draft_id`.
    - Finalization never submits a formal real Kaihong order.
  artifacts:
    - `app/services/clarification.py` owns session persistence, graph invocation, model result validation, draft merge, conflict resolution, and finalization orchestration.
    - `app/api/agent_tasks.py` exposes protected clarification and finalize endpoints.
    - `tests/test_clarification_api.py` covers the full API loop.
    - Existing `tests/test_agent_tasks.py` remains green and includes OpenAPI coverage for new routes.
  key_links:
    - Uses Plan 01 `AgentGraphSessionRecord`, privacy service, DeepSeek client, and LangGraph graph.
    - Updates `AgentTaskRecord.draft_preview_json` and task status through existing service boundaries.
    - Calls `app/services/mock_kaihong.py:create_draft` for final save.
---

<objective>
Implement the Phase 6 user-facing clarification and finalization loop.

Purpose: Let authenticated task owners get clarification questions, submit answers, update the draft preview, transition complete tasks to `ready_for_review`, and finalize the draft through Mock Kaihong.
Output: Clarification service, protected APIs, draft merge/finalization logic, task events, OpenAPI coverage, and end-to-end API tests.
</objective>

<execution_context>
@C:/Users/qirui.huang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/qirui.huang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-clarification-and-draft-finalization/06-CONTEXT.md
@.planning/phases/06-clarification-and-draft-finalization/06-RESEARCH.md
@.planning/phases/06-clarification-and-draft-finalization/06-01-PLAN.md
@.planning/phases/05-mock-ocr-and-field-extraction/05-01-SUMMARY.md
@.planning/phases/02-mock-kaihong-business-api/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add clarification service session orchestration</name>
  <files>app/services/clarification.py, tests/test_clarification_api.py</files>
  <read_first>app/services/agent_tasks.py, app/services/clarification_graph.py, app/models/agent_graph_session.py, app/schemas/order_draft.py</read_first>
  <action>
    Create `app/services/clarification.py`.
    Add service errors that carry HTTP-friendly status codes.
    Implement a function such as `get_or_create_clarification(session, task_id, current_username)`.

    Behavior:
    - Return not found/inaccessible for tasks not owned by the current user.
    - Require a populated `draft_preview_json`.
    - Allow clarification for `need_more_info`; return completion state for `ready_for_review`; reject `created`, `extracting`, `finalized`, and `failed` with clear errors where appropriate.
    - Load or create an `AgentGraphSessionRecord`.
    - Build a redacted graph state from the draft preview, conflicts, and answer history.
    - Invoke the LangGraph question-generation path.
    - Persist `state_json`, `current_question_json`, `privacy_map_json`, and timestamps.
    - Return `ClarificationStatusResponse`.
    - Record business-level events such as `clarification_started` and `clarification_question_generated`.

    Add integration tests using monkeypatched DeepSeek/graph behavior to prove a task in `need_more_info` returns a structured question.
  </action>
  <verify>python -m pytest tests/test_clarification_api.py::test_get_clarification_returns_current_question -q</verify>
  <acceptance_criteria>
    - Task ownership is enforced.
    - Existing draft preview is required.
    - A graph session is created separately from the task record.
    - Current question is persisted and returned.
    - Model calls use redacted context from Plan 01.
  </acceptance_criteria>
  <done>Users can retrieve the current clarification state for a task that needs more information.</done>
</task>

<task type="auto">
  <name>Task 2: Implement answer submission, validation, and draft merge</name>
  <files>app/services/clarification.py, app/services/agent_tasks.py, tests/test_clarification_api.py</files>
  <read_first>app/schemas/order_draft.py, app/schemas/clarification.py, app/services/extraction.py, app/services/agent_tasks.py</read_first>
  <action>
    In `app/services/clarification.py`, implement `submit_clarification_answer(session, task_id, current_username, answer_text)`.

    Behavior:
    - Validate task ownership and active clarification state.
    - Append the user answer to session history.
    - Redact the answer before model parsing.
    - Invoke the LangGraph answer-parsing/merge path.
    - Validate parsed field names against `ALL_DRAFT_FIELDS`.
    - Validate parsed values against `OrderDraftFields` types using Pydantic.
    - Merge accepted values into the existing `OrderDraftPreview`.
    - Add/update field evidence for user-provided values with a source such as `user_clarification`.
    - Recompute `missing_fields` from `REQUIRED_DRAFT_FIELDS`.
    - Resolve conflicts only when the answer explicitly chooses or overwrites the conflict field.
    - Persist updated `draft_preview_json`, `missing_fields_json`, session state, current question, answer history, and privacy map.
    - Transition task from `need_more_info` to `ready_for_review` when no required fields or blocking conflicts remain.
    - Keep task in `need_more_info` and return the next question when more clarification is required.
    - Record business-level events such as `clarification_answer_submitted`, `draft_fields_merged`, and `clarification_completed`.

    Add tests for:
    - missing required fields are filled from a natural language answer.
    - grouped questions can update multiple fields.
    - invalid model field names are rejected and do not mutate the draft.
    - conflicts include candidate values and can be resolved by answer.
    - completion transitions to `ready_for_review`.
  </action>
  <verify>python -m pytest tests/test_clarification_api.py -q</verify>
  <acceptance_criteria>
    - CLAR-02 and CLAR-03 are implemented through authenticated answer submission.
    - Backend validates all model output before draft mutation.
    - Missing required fields are recalculated after merge.
    - Conflicts remain until explicitly resolved.
    - Task reaches `ready_for_review` only when the draft is complete enough.
  </acceptance_criteria>
  <done>Clarification answers can update and validate the draft preview safely.</done>
</task>

<task type="auto">
  <name>Task 3: Expose clarification endpoints</name>
  <files>app/api/agent_tasks.py, app/schemas/clarification.py, tests/test_clarification_api.py, tests/test_agent_tasks.py</files>
  <read_first>app/api/agent_tasks.py, app/api/mock_kaihong.py, app/services/clarification.py, app/schemas/agent_tasks.py</read_first>
  <action>
    Extend `app/api/agent_tasks.py` under the existing protected router:
    - `GET /agent/tasks/{task_id}/clarification`, response model `ClarificationStatusResponse`.
    - `POST /agent/tasks/{task_id}/clarification/answers`, response model `ClarificationAnswerResponse`.

    Reuse `get_current_user` and `get_session`.
    Convert clarification service errors to `HTTPException`.
    Keep route names and OpenAPI tags consistent with existing Agent task routes.

    Update `AgentTaskResponse.questions` if useful so task detail can include the current question when a session exists; keep backward compatibility for tasks without clarification sessions.

    Add OpenAPI tests proving both clarification routes are present.
  </action>
  <verify>python -c "from app.main import app; paths=app.openapi()['paths']; print('/agent/tasks/{task_id}/clarification' in paths, '/agent/tasks/{task_id}/clarification/answers' in paths)"</verify>
  <acceptance_criteria>
    - Clarification routes require bearer auth.
    - Missing/inaccessible tasks return 404.
    - Invalid task states return clear 400 errors.
    - OpenAPI documents request/response contracts.
    - Existing task routes continue working.
  </acceptance_criteria>
  <done>Clarification can be driven through protected API endpoints.</done>
</task>

<task type="auto">
  <name>Task 4: Implement finalize and Mock Kaihong draft save</name>
  <files>app/models/agent_task.py, app/db/session.py, app/schemas/agent_tasks.py, app/services/agent_tasks.py, app/services/clarification.py, app/api/agent_tasks.py, tests/test_clarification_api.py</files>
  <read_first>app/services/mock_kaihong.py, app/schemas/mock_kaihong.py, app/models/agent_task.py, app/services/agent_tasks.py</read_first>
  <action>
    Add finalization support.
    If useful, extend `AgentTaskRecord` with nullable `finalized_draft_id` and add SQLite additive migration for existing local databases.

    Add a response schema such as `AgentTaskFinalizeResponse` with:
    - `task_id`
    - `status`
    - `draft_id`
    - `draft_preview`
    - `source_file_ids`
    - `events` or audit metadata summary

    Implement `finalize_agent_task(session, task_id, current_username)` in `app/services/clarification.py` or `app/services/agent_tasks.py`.
    Behavior:
    - Require task ownership.
    - Require status `ready_for_review`.
    - Require a populated `draft_preview`.
    - Reject if required fields are still missing or blocking conflicts remain.
    - Map `OrderDraftPreview` into `MockDraftCreateRequest`:
      - `customer_id` from task customer ID or a safe fallback if the current mock contract requires it.
      - `created_by` from current user.
      - `source_documents` from task uploaded files.
      - one `MockDraftOrderItem` containing draft field values and empty missing fields.
      - `payload` containing field evidence, conflicts, task id, and source metadata.
      - `source = "kaihom-agent-v1"`.
    - Call `mock_kaihong.create_draft`.
    - Store the returned `draft_id` on the task if the new column is added.
    - Transition task to `finalized`.
    - Record a `draft_finalized` business event with `draft_id`.

    Add `POST /agent/tasks/{task_id}/finalize` protected route.
    Add tests:
    - finalize rejects `need_more_info`.
    - finalize saves a Mock Kaihong draft for `ready_for_review`.
    - response includes `draft_id`, field values, source file references, and audit metadata.
    - finalized task cannot be finalized again unless the implementation deliberately makes idempotency explicit.
  </action>
  <verify>python -m pytest tests/test_clarification_api.py::test_finalize_ready_task_saves_mock_kaihong_draft -q</verify>
  <acceptance_criteria>
    - DRAFT-01 through DRAFT-03 are implemented.
    - Finalization only works from `ready_for_review`.
    - Mock Kaihong draft persistence is reused rather than bypassed.
    - Finalized response includes `draft_id`, fields, source file references, and audit metadata.
    - Task status becomes `finalized` and a business event records the save.
  </acceptance_criteria>
  <done>Ready drafts can be finalized into the Mock Kaihong draft boundary.</done>
</task>

<task type="auto">
  <name>Task 5: Verify contracts, OpenAPI, and regression safety</name>
  <files>tests/test_clarification_api.py, tests/test_agent_tasks.py</files>
  <read_first>app/api/agent_tasks.py, app/services/clarification.py, app/services/agent_tasks.py, app/schemas/clarification.py, .planning/phases/06-clarification-and-draft-finalization/06-CONTEXT.md</read_first>
  <action>
    Add or refine regression tests for:
    - unauthenticated clarification/finalize routes return 401.
    - task ownership is enforced.
    - OpenAPI includes clarification and finalize routes.
    - task event history includes clarification and finalization business events.
    - existing upload, extraction, and task tests continue passing.
    - no test requires real DeepSeek credentials or live network access.
    - no image/PDF multimodal recognition API or implementation was added in Phase 6.

    If DRAFT-04 needs future Java/Kaihong-facing contracts, document the finalization request/response in OpenAPI via Pydantic schemas and route descriptions; do not implement real Java integration.
  </action>
  <verify>python -m pytest tests/test_privacy.py tests/test_deepseek_client.py tests/test_clarification_graph.py tests/test_clarification_api.py tests/test_agent_tasks.py -q; python -m pytest -q</verify>
  <acceptance_criteria>
    - CLAR-01 through CLAR-04 are covered.
    - DRAFT-01 through DRAFT-04 are covered.
    - Full test suite passes.
    - OpenAPI documents the new contracts.
    - Phase 6 remains text-clarification only.
  </acceptance_criteria>
  <done>Phase 6 is verified end to end and ready for the H5/mobile phase to consume.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `python -m pytest tests/test_clarification_api.py -q`.
- [ ] Run `python -m pytest tests/test_privacy.py tests/test_deepseek_client.py tests/test_clarification_graph.py tests/test_clarification_api.py tests/test_agent_tasks.py -q`.
- [ ] Run `python -m pytest -q`.
- [ ] Run `python -c "from app.main import app; paths=app.openapi()['paths']; print('/agent/tasks/{task_id}/clarification' in paths, '/agent/tasks/{task_id}/clarification/answers' in paths, '/agent/tasks/{task_id}/finalize' in paths)"`.
- [ ] Manually inspect one complete task flow: upload -> create task -> extract -> clarification -> answer -> ready_for_review -> finalize.
- [ ] Confirm tests do not require real DeepSeek credentials.
- [ ] Confirm Phase 6 did not implement image/PDF multimodal field recognition.
</verification>

<success_criteria>

- CLAR-01: system generates user-facing clarification questions for missing required fields and conflicts.
- CLAR-02: user can submit answers through a protected endpoint.
- CLAR-03: system validates and merges answers into the draft preview.
- CLAR-04: system transitions to `ready_for_review` once required fields are complete and blocking conflicts are resolved.
- DRAFT-01: user can finalize a ready draft.
- DRAFT-02: finalized draft is saved locally through the Mock Kaihong API boundary.
- DRAFT-03: finalized response includes `draft_id`, field values, source file references, and audit metadata.
- DRAFT-04: OpenAPI shows the finalization request/response contract for future Kaihong-facing integration.

</success_criteria>

<output>
After completion, create `.planning/phases/06-clarification-and-draft-finalization/06-02-SUMMARY.md`.
</output>

## PLANNING COMPLETE
