---
phase: 06-clarification-and-draft-finalization
plan: 01
type: execute
wave: 1
depends_on:
  - 05-mock-ocr-and-field-extraction
files_modified:
  - pyproject.toml
  - app/core/config.py
  - app/models/agent_graph_session.py
  - app/db/session.py
  - app/schemas/clarification.py
  - app/services/privacy.py
  - app/services/deepseek_client.py
  - app/services/clarification_graph.py
  - tests/test_privacy.py
  - tests/test_deepseek_client.py
  - tests/test_clarification_graph.py
autonomous: true
requirements:
  - CLAR-01
  - CLAR-02
  - CLAR-03
must_haves:
  truths:
    - D-01 Phase 6 consumes the existing Phase 5 `draft_preview`; it does not implement image/PDF multimodal field recognition.
    - D-02 LangGraph orchestrates the clarification loop, but deterministic backend code remains authoritative.
    - D-03 DeepSeek is configured directly for text-only question generation and answer parsing; do not build a generic provider abstraction.
    - D-04 Draft context and user answers are minimally redacted before DeepSeek calls.
    - D-05 LangGraph state is persisted in a separate `agent_graph_sessions` table, not on `AgentTaskRecord`.
    - D-08 DeepSeek failures retry once and remain recoverable.
    - Tests must not require real DeepSeek credentials or network access.
  artifacts:
    - `pyproject.toml` includes required runtime dependencies for LangGraph and DeepSeek HTTP access.
    - `app/core/config.py` exposes DeepSeek and clarification settings through the existing `KAIHOM_` prefix.
    - `app/models/agent_graph_session.py` defines the graph session SQLModel table.
    - `app/schemas/clarification.py` defines structured question, answer, graph state, and parsed-answer schemas.
    - `app/services/privacy.py` redacts sensitive draft/answer text and returns a local privacy map.
    - `app/services/deepseek_client.py` performs direct DeepSeek chat-completion calls with strict JSON parsing hooks.
    - `app/services/clarification_graph.py` wires the LangGraph state machine without owning database transactions.
  key_links:
    - Clarification consumes `OrderDraftPreview` from `app/schemas/order_draft.py`.
    - Graph sessions belong to `AgentTaskRecord.id`.
    - DeepSeek calls use redacted payloads produced by `app/services/privacy.py`.
    - Plan 02 will connect these foundations to task APIs and finalization.
---

<objective>
Build the Phase 6 clarification foundation.

Purpose: Add the LangGraph state, DeepSeek text client, redaction layer, graph-session persistence, and schemas needed for a recoverable human-in-the-loop clarification workflow.
Output: Dependencies, settings, SQLModel graph session table, clarification schemas, privacy service, DeepSeek client, LangGraph wiring, and focused tests.
</objective>

<execution_context>
@C:/Users/qirui.huang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/qirui.huang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/06-clarification-and-draft-finalization/06-CONTEXT.md
@.planning/phases/06-clarification-and-draft-finalization/06-RESEARCH.md
@.planning/phases/05-mock-ocr-and-field-extraction/05-01-SUMMARY.md
@.planning/phases/04-agent-task-state-machine/04-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add DeepSeek/LangGraph dependencies and settings</name>
  <files>pyproject.toml, app/core/config.py</files>
  <read_first>pyproject.toml, app/core/config.py, .planning/phases/06-clarification-and-draft-finalization/06-CONTEXT.md</read_first>
  <action>
    Update runtime dependencies for Phase 6:
    - Add `langgraph`.
    - Add `langchain-core` only if the implementation uses LangChain prompt/parser utilities.
    - Promote or add `httpx` as a runtime dependency for direct DeepSeek HTTP calls.

    Update `Settings` in `app/core/config.py`:
    - `deepseek_api_key: str | None = None`
    - `deepseek_base_url: str = "https://api.deepseek.com"`
    - `deepseek_model: str = "deepseek-v4-flash"` unless a safer locally documented default is chosen.
    - `deepseek_timeout_seconds: float = 30.0`
    - `deepseek_max_retries: int = 1`
    - `clarification_max_rounds: int = 8`

    Keep the existing `KAIHOM_` prefix; do not read secrets outside settings.
  </action>
  <verify>python -c "from app.core.config import Settings; s=Settings(); print(s.deepseek_base_url, s.deepseek_model, s.deepseek_max_retries)"</verify>
  <acceptance_criteria>
    - Phase 6 dependencies are declared.
    - DeepSeek settings are available through `KAIHOM_` environment variables.
    - A missing API key does not break app import or tests.
    - No generic provider registry is introduced.
  </acceptance_criteria>
  <done>DeepSeek and LangGraph can be configured without hard-coded secrets.</done>
</task>

<task type="auto">
  <name>Task 2: Add graph session persistence and clarification schemas</name>
  <files>app/models/agent_graph_session.py, app/db/session.py, app/schemas/clarification.py</files>
  <read_first>app/models/agent_task.py, app/db/session.py, app/schemas/order_draft.py, app/schemas/agent_tasks.py</read_first>
  <action>
    Create `app/models/agent_graph_session.py`.
    Define `AgentGraphSessionRecord` as a SQLModel table with:
    - `id` primary key, such as `graph_<12 hex>`.
    - indexed `task_id`.
    - `graph_thread_id`.
    - `status`, defaulting to `active`.
    - nullable `state_json`.
    - nullable `current_question_json`.
    - nullable `answer_history_json`.
    - nullable `privacy_map_json`.
    - `created_at` and `updated_at` using the existing UTC timestamp style.

    Update `app/db/session.py` to import the new model module before table creation.
    If needed, add a small SQLite-safe table/column migration helper following the Phase 5 additive migration style.

    Create `app/schemas/clarification.py` with Pydantic models:
    - `ClarificationFieldRequest` with field name, label, reason, current value, evidence, and candidate values.
    - `ClarificationQuestion` with question id, text, requested fields, conflict metadata, and round number.
    - `ClarificationStatusResponse` with task id, task status, session id, current question, answer history, missing fields, conflicts, and completion flags.
    - `ClarificationAnswerRequest` with `answer_text`.
    - `ClarificationAnswerResponse` with updated status, parsed fields, remaining missing fields, remaining conflicts, and optional next question.
    - `ParsedClarificationAnswer` for validated model output.
    - `ClarificationGraphState` for the graph's in-memory state.
  </action>
  <verify>python -c "from app.models.agent_graph_session import AgentGraphSessionRecord; from app.schemas.clarification import ClarificationQuestion; print(AgentGraphSessionRecord.__name__, ClarificationQuestion.__name__)"</verify>
  <acceptance_criteria>
    - `agent_graph_sessions` is represented by a separate model.
    - The DB session imports the new model.
    - Clarification schemas keep question/answer state structured.
    - Schemas can represent grouped missing-field questions and conflict choices.
  </acceptance_criteria>
  <done>Clarification state has durable storage and typed API/service contracts.</done>
</task>

<task type="auto">
  <name>Task 3: Implement minimal privacy redaction</name>
  <files>app/services/privacy.py, tests/test_privacy.py</files>
  <read_first>app/schemas/order_draft.py, .planning/phases/06-clarification-and-draft-finalization/06-CONTEXT.md</read_first>
  <action>
    Create `app/services/privacy.py`.
    Implement deterministic redaction helpers that accept draft fields, conflicts, and user answer text.
    Redact at least:
    - customer names and other known party names from draft fields.
    - phone-like strings.
    - detailed address values from draft fields.

    Return:
    - redacted payload suitable for DeepSeek.
    - privacy map JSON-serializable dictionary for local storage.

    Use stable placeholders such as `[CUSTOMER_1]`, `[PHONE_1]`, and `[ADDRESS_1]`.
    Do not send or log the privacy map in model prompts.
    Add tests proving sensitive values are replaced and non-sensitive cargo/ports/weights can remain.
  </action>
  <verify>python -m pytest tests/test_privacy.py -q</verify>
  <acceptance_criteria>
    - Customer/party names, phones, and detailed addresses are redacted.
    - Redaction is deterministic enough for tests and session persistence.
    - Privacy map remains local and JSON-serializable.
    - Redaction does not destroy every useful logistics value.
  </acceptance_criteria>
  <done>DeepSeek inputs can be built from minimally redacted task context.</done>
</task>

<task type="auto">
  <name>Task 4: Implement direct DeepSeek client</name>
  <files>app/services/deepseek_client.py, tests/test_deepseek_client.py</files>
  <read_first>app/core/config.py, app/schemas/clarification.py, .planning/phases/06-clarification-and-draft-finalization/06-RESEARCH.md</read_first>
  <action>
    Create `app/services/deepseek_client.py`.
    Implement a thin direct client around DeepSeek chat completions using settings:
    - `deepseek_api_key`
    - `deepseek_base_url`
    - `deepseek_model`
    - timeout
    - max retries

    Provide functions or methods for:
    - generating a clarification question from redacted draft context.
    - parsing a user answer into strict JSON matching `ParsedClarificationAnswer`.

    Requirements:
    - Do not build a generic provider abstraction.
    - Do not call DeepSeek when the API key is missing; raise a clear recoverable service error.
    - Request non-streaming JSON-oriented responses.
    - Retry once for transient HTTP failures or invalid JSON when configured.
    - Make the HTTP client injectable or monkeypatchable for tests.

    Add tests using fake HTTP responses. Cover success, missing API key, retry after invalid JSON, and final recoverable failure.
  </action>
  <verify>python -m pytest tests/test_deepseek_client.py -q</verify>
  <acceptance_criteria>
    - DeepSeek client uses configured base URL, model, and API key.
    - Tests do not use network or real credentials.
    - Invalid JSON is retried once.
    - Final failures are recoverable errors, not process crashes.
    - There is no provider registry or multimodal recognition client.
  </acceptance_criteria>
  <done>Phase 6 can call DeepSeek directly for text-only clarification intelligence.</done>
</task>

<task type="auto">
  <name>Task 5: Wire the LangGraph clarification state machine</name>
  <files>app/services/clarification_graph.py, tests/test_clarification_graph.py</files>
  <read_first>app/schemas/clarification.py, app/services/deepseek_client.py, app/services/privacy.py, app/schemas/order_draft.py</read_first>
  <action>
    Create `app/services/clarification_graph.py`.
    Build a minimal LangGraph state graph with nodes equivalent to:
    - inspect draft
    - generate question
    - wait for answer
    - parse answer
    - merge answer intent into graph state
    - decide next

    Keep graph node functions pure or near-pure where possible.
    Do not perform SQLModel session commits inside graph nodes.
    Do not directly change `AgentTaskRecord` inside the graph.
    Return structured graph output for `app/services/clarification.py` to persist in Plan 02.

    Add tests using fake DeepSeek behavior:
    - missing fields produce a structured question.
    - conflict fields produce a question with candidate values.
    - answer parsing yields structured field updates.
    - graph marks itself complete when no missing fields/conflicts remain.
  </action>
  <verify>python -m pytest tests/test_clarification_graph.py -q</verify>
  <acceptance_criteria>
    - LangGraph is actually used for workflow routing.
    - The graph can pause around user input.
    - Graph output is structured and persistable.
    - Database transactions remain outside graph node internals.
  </acceptance_criteria>
  <done>LangGraph can orchestrate the clarification state without owning persistence.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `python -m pytest tests/test_privacy.py tests/test_deepseek_client.py tests/test_clarification_graph.py -q`.
- [ ] Run `python -c "from app.models.agent_graph_session import AgentGraphSessionRecord; print(AgentGraphSessionRecord.__tablename__ if hasattr(AgentGraphSessionRecord, '__tablename__') else AgentGraphSessionRecord.__name__)"`.
- [ ] Run `python -m pytest -q`.
- [ ] Confirm no image/PDF multimodal recognition implementation was added.
- [ ] Confirm tests pass without real DeepSeek credentials.
</verification>

<success_criteria>

- LangGraph and DeepSeek dependencies/settings are ready.
- `agent_graph_sessions` exists as separate persistence from `AgentTaskRecord`.
- Clarification API/service schemas exist.
- Minimal privacy redaction protects model inputs.
- Direct DeepSeek text client supports question generation and answer parsing with retry.
- LangGraph workflow can produce questions, parse answers, and decide continuation in tests.
- CLAR-01 through CLAR-03 foundation is ready for API integration in Plan 02.

</success_criteria>

<output>
After completion, create `.planning/phases/06-clarification-and-draft-finalization/06-01-SUMMARY.md`.
</output>

## PLANNING COMPLETE
