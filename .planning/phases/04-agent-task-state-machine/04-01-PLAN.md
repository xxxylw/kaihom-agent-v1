---
phase: 04-agent-task-state-machine
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - app/models/agent_task.py
  - app/schemas/agent_tasks.py
  - app/services/agent_tasks.py
  - app/api/agent_tasks.py
  - app/db/session.py
  - app/main.py
  - tests/test_agent_tasks.py
autonomous: true
requirements:
  - TASK-01
  - TASK-02
  - TASK-03
  - TASK-04
must_haves:
  truths:
    - `POST /agent/tasks` creates an authenticated Agent task from one or more uploaded `file_id` references.
    - Created tasks start with `status == "created"` and do not pretend extraction has begun.
    - `customer_id` is optional on task creation.
    - One task supports multiple uploaded files.
    - Task creation links uploaded file metadata to the new `task_id`.
    - `GET /agent/tasks/{task_id}` returns current task status, safe file references, empty `draft_preview`, empty `questions`, timestamps, and error metadata fields.
    - Task events are durable and queryable.
    - Phase 4 records business-level events, not OCR/LLM/LangGraph internals.
    - No broad public endpoint allows arbitrary status mutation.
    - LangChain and LangGraph are not runtime dependencies in this phase.
    - A service boundary exists so future LangGraph/LangChain orchestration can update task status/events without changing the public task API.
  artifacts:
    - `app/models/agent_task.py` defines Agent task and event SQLModel records.
    - `app/schemas/agent_tasks.py` defines task create/detail/event response schemas.
    - `app/services/agent_tasks.py` defines task creation, lookup, event, and transition helpers.
    - `app/api/agent_tasks.py` exposes protected Agent task routes.
    - `tests/test_agent_tasks.py` verifies task creation, status query, events, invalid file handling, auth, OpenAPI, and transition rules.
  key_links:
    - Agent task routes reuse `get_current_user` from `app/api/mock_kaihong.py`.
    - Agent task routes reuse `get_session` from `app/db/session.py`.
    - Task creation validates and links `UploadedFileRecord` entries from Phase 3.
    - `app/main.py` includes the Agent task router.
    - `app/db/session.py` imports the new model module before table creation.
---

<objective>
Create the Phase 4 Agent task state machine foundation for Kaihom Agent v1.

Purpose: Give the mock logistics Agent workflow a durable `task_id`, product-facing task status, event history, and file linkage layer that later OCR, extraction, clarification, and draft finalization phases can build on.
Output: SQLModel records, service helpers, protected FastAPI endpoints, and tests for Agent task creation/query/events/failure handling.
</objective>

<execution_context>
@C:/Users/qirui.huang/.codex/get-shit-done/workflows/execute-plan.md
@C:/Users/qirui.huang/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/04-agent-task-state-machine/04-CONTEXT.md
@.planning/phases/04-agent-task-state-machine/04-RESEARCH.md
@.planning/phases/03-file-upload-and-local-storage/03-CONTEXT.md
@.planning/phases/03-file-upload-and-local-storage/03-01-SUMMARY.md
@.planning/phases/02-mock-kaihong-business-api/02-01-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add task/event persistence models and schemas</name>
  <files>app/models/agent_task.py, app/schemas/agent_tasks.py, app/db/session.py</files>
  <read_first>app/models/draft.py, app/models/uploaded_file.py, app/schemas/uploads.py, app/db/session.py, .planning/phases/04-agent-task-state-machine/04-CONTEXT.md</read_first>
  <action>
    Create `app/models/agent_task.py`.
    Define a shared UTC timestamp helper or reuse the existing timestamp style from prior models.
    Add `AgentTaskRecord` as a SQLModel table with at least:
    - `id` primary key.
    - `created_by`.
    - nullable `customer_id`.
    - `status`, defaulting to `created`.
    - storage for multiple file IDs, such as `file_ids_json`.
    - nullable `error_code`.
    - nullable `error_message`.
    - `created_at`.
    - `updated_at`.
    Add `AgentTaskEventRecord` as a SQLModel table with at least:
    - `id` primary key.
    - indexed `task_id`.
    - `event_type`.
    - nullable `actor`.
    - nullable `from_status`.
    - nullable `to_status`.
    - nullable `message`.
    - nullable details storage, such as `details_json`.
    - `created_at`.
    Keep records ASCII and business-level.
    Update `app/db/session.py` to import the new model module so SQLModel table creation includes Agent task tables.
    Create `app/schemas/agent_tasks.py` with Pydantic models for:
    - `AgentTaskCreateRequest` with required `file_ids: list[str]` and optional `customer_id`.
    - `AgentTaskEventResponse`.
    - `AgentTaskResponse` with `task_id`, `status`, `customer_id`, `file_ids`, safe file metadata list, empty/nullable `draft_preview`, `questions`, error metadata, and timestamps.
    - `AgentTaskEventsResponse` if using a wrapper response for events.
  </action>
  <verify>python -c "from app.models.agent_task import AgentTaskRecord, AgentTaskEventRecord; from app.schemas.agent_tasks import AgentTaskCreateRequest; print(AgentTaskRecord.__name__, AgentTaskEventRecord.__name__, AgentTaskCreateRequest.__name__)"</verify>
  <acceptance_criteria>
    - Agent task and event SQLModel records exist.
    - Agent task create/detail/event schemas exist.
    - `app/db/session.py` imports the new model module.
    - Status defaults to `created`.
    - Schema supports multiple file IDs and optional `customer_id`.
  </acceptance_criteria>
  <done>Persistence and schema foundation exists for durable task status and event history.</done>
</task>

<task type="auto">
  <name>Task 2: Implement Agent task service and state transition helpers</name>
  <files>app/services/agent_tasks.py</files>
  <read_first>app/services/uploads.py, app/services/mock_kaihong.py, app/models/uploaded_file.py, .planning/phases/04-agent-task-state-machine/04-RESEARCH.md</read_first>
  <action>
    Create `app/services/agent_tasks.py`.
    Define business-level status constants or an enum-like set for `created`, `extracting`, `need_more_info`, `ready_for_review`, `finalized`, and `failed`.
    Define business-level event constants for at least `task_created`, `files_attached`, `status_changed`, and `task_failed`.
    Add a domain exception such as `AgentTaskError` carrying a message and HTTP-friendly status code.
    Implement `create_agent_task(session, file_ids, created_by, customer_id=None)`:
    - Reject empty `file_ids`.
    - Validate all referenced `UploadedFileRecord` rows exist before creating the task.
    - Prefer validating that files are accessible to the current user by comparing `uploaded_by` where existing data allows it.
    - Create a `task_<12 hex>` task in `created`.
    - Link each uploaded file record to the new task ID and update timestamps.
    - Record `task_created` and `files_attached` events.
    - Commit atomically enough that invalid file batches do not create partial tasks.
    - Return an `AgentTaskResponse`.
    Implement `get_agent_task(session, task_id, current_username)` that returns task detail with safe uploaded file metadata or `None`.
    Implement `list_task_events(session, task_id, current_username)` that returns ordered events or `None` when the task is missing/inaccessible.
    Implement an internal `transition_task_status(session, task_id, new_status, actor, message=None, error_code=None, error_message=None)` helper:
    - Enforce allowed transitions.
    - Record `status_changed` events.
    - Record/update failure metadata for `failed`.
    - Do not expose this helper directly as an arbitrary public API in Phase 4.
    Keep LangChain/LangGraph out of runtime imports. Name or structure service boundaries so future workflow engines can call these helpers.
  </action>
  <verify>python -c "from app.services import agent_tasks; print(agent_tasks.CREATED_STATUS)"</verify>
  <acceptance_criteria>
    - Empty task creation is rejected.
    - Missing file IDs are rejected before a task is created.
    - Multiple file IDs can be linked to one task.
    - Events are recorded for creation and file attachment.
    - Invalid state transitions raise a clear service error.
    - `finalized` does not transition back to active states.
    - No LangChain/LangGraph dependency is imported.
  </acceptance_criteria>
  <done>Service layer can create/query tasks, record events, and enforce deterministic status transitions behind a future workflow boundary.</done>
</task>

<task type="auto">
  <name>Task 3: Expose protected Agent task API routes</name>
  <files>app/api/agent_tasks.py, app/main.py</files>
  <read_first>app/api/uploads.py, app/api/mock_kaihong.py, app/main.py, app/schemas/agent_tasks.py</read_first>
  <action>
    Create `app/api/agent_tasks.py`.
    Define an `APIRouter` with prefix `/agent/tasks` and a clear tag such as `Agent Tasks`.
    Reuse `get_current_user` from `app/api/mock_kaihong.py`.
    Reuse `get_session` from `app/db/session.py`.
    Add `POST ""` with `response_model=AgentTaskResponse` and HTTP 201:
    - Calls `agent_tasks.create_agent_task`.
    - Converts service errors to `HTTPException`.
    Add `GET "/{task_id}"` with `response_model=AgentTaskResponse`:
    - Returns 404 when missing/inaccessible.
    Add `GET "/{task_id}/events"` with an events response model:
    - Returns 404 when missing/inaccessible.
    Do not add a public arbitrary status patch endpoint.
    Update `app/main.py` to include the Agent task router.
  </action>
  <verify>python -c "from app.main import app; print('/agent/tasks' in app.openapi()['paths'])"</verify>
  <acceptance_criteria>
    - `POST /agent/tasks` is protected by bearer auth.
    - `GET /agent/tasks/{task_id}` is protected by bearer auth.
    - `GET /agent/tasks/{task_id}/events` is protected by bearer auth.
    - OpenAPI includes the Agent task routes.
    - No public route allows arbitrary status mutation.
  </acceptance_criteria>
  <done>Agent task APIs are documented and connected to the deterministic service layer.</done>
</task>

<task type="auto">
  <name>Task 4: Add Phase 4 API and service tests</name>
  <files>tests/test_agent_tasks.py</files>
  <read_first>tests/test_uploads.py, tests/test_mock_kaihong.py, app/services/agent_tasks.py, .planning/phases/04-agent-task-state-machine/04-CONTEXT.md</read_first>
  <action>
    Create `tests/test_agent_tasks.py` using `TestClient(app)` and the existing mock login pattern.
    Add tests for:
    1. Agent task routes require bearer auth.
    2. Creating a task from one uploaded file returns HTTP 201, `status == "created"`, a `task_` ID, the file ID, empty `questions`, and null/empty `draft_preview`.
    3. Creating a task from multiple uploaded files links all file metadata to the same task ID.
    4. Creating a task with optional `customer_id` preserves that customer ID.
    5. Creating a task with an unknown file ID returns a clear 404 or 400 and does not create a partial task.
    6. `GET /agent/tasks/{task_id}` returns current status and safe file metadata without absolute local paths.
    7. `GET /agent/tasks/{task_id}/events` returns `task_created` and `files_attached` events in chronological order.
    8. Internal service transition rules allow valid transitions such as `created -> extracting` and reject invalid transitions such as `finalized -> extracting`.
    9. OpenAPI includes `/agent/tasks`, `/agent/tasks/{task_id}`, and `/agent/tasks/{task_id}/events`.
    Prefer using existing upload endpoints in setup helpers so tests exercise the integration with Phase 3.
  </action>
  <verify>python -m pytest tests/test_agent_tasks.py -q</verify>
  <acceptance_criteria>
    - Tests cover TASK-01 through TASK-04.
    - Tests prove tasks start in `created`.
    - Tests prove multiple file IDs can be attached to one task.
    - Tests prove events are queryable.
    - Tests prove invalid transitions are rejected.
    - Tests prove no unauthenticated task access.
  </acceptance_criteria>
  <done>Phase 4 behavior is covered by focused API/service tests.</done>
</task>

</tasks>

<verification>
Before declaring plan complete:
- [ ] Run `python -c "from app.main import app; print(sorted(path for path in app.openapi()['paths'] if path.startswith('/agent/tasks')))"`.
- [ ] Run `python -m pytest tests/test_agent_tasks.py -q`.
- [ ] Run `python -m pytest`.
- [ ] If SQLite or pytest cache writes fail due to sandbox disk I/O, rerun the same verification with appropriate local write permissions rather than weakening the tests.
</verification>

<success_criteria>

- TASK-01 through TASK-04 are addressed by concrete files and checks.
- Authenticated users can create Agent tasks from uploaded file references.
- Agent task status is durable, product-facing, and queryable.
- Task events are durable and queryable.
- Failure and invalid transition behavior is represented clearly.
- Uploaded files are linked to Agent task IDs.
- The implementation does not add LangChain/LangGraph dependencies in Phase 4.
- A clean service boundary remains for Phase 5/6 to connect future LangGraph/LangChain orchestration.

</success_criteria>

<output>
After completion, create `.planning/phases/04-agent-task-state-machine/04-01-SUMMARY.md`.
</output>

## PLANNING COMPLETE
